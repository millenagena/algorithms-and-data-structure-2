#include<stdio.h>
#include<stdlib.h>

int partition(int v[], int left, int right){

    int i, fim = -1;

    for(int i=left; i<=right; i++){ 
        if(v[i] > 1000000){
            fim++;
            
        }
    }

}